# 官方更新合并记录（2026-09-22）

## 基线与回退点

- 原本地版本：`21ddc20`。
- 本地媒体库修复提交：`560a6d7`。
- 原有专用 Flow 工作标签页改动提交：`3041879`。
- 合并前备份分支：`backup/pre-upstream-sync-20260922`。
- 官方目标：`upstream/main` 的 `d7977fd`，应用版本 1.3.1，扩展版本 0.3.2。
- 数据及设置备份：`output/backups/pre-upstream-sync-20260922/`。

采用真正的 Git merge，保留双方提交历史；没有 reset 到官方分支，也没有推送远程仓库。

## 冲突处理

| 文件 | 兼容方式 |
|---|---|
| `.gitignore` | 合并本地设置、构建产物、日志规则与官方会话目录规则；忽略本机崩溃转储。 |
| `agent/services/flow_client.py` | 以官方新版生成接口为基础，加入本地完整媒体列表、原文/译文、媒体类型、缓存签名地址解析。遵循官方移除失效旧 REST transport 的方案。 |
| `agent/api/flow.py` | 官方 count/seed/reference/Omni/export 能力与本地项目媒体、上传、代理下载、JSON upscale 响应共存。静态媒体路由优先于动态媒体 ID 路由。 |
| `agent/sdk/services/operations.py` | 保留本地画幅映射与模型覆盖，使用官方新版生成及轮询行为。 |
| `dashboard/src/App.tsx` | 官方 AI provider 配置页使用 `/settings`；保留本地 `/settings/llm`、`/settings/llm-test` 和所有生图工坊入口。 |
| `extension/background.js` | 保留本地 9223 端口与专用工作标签页选择，融合官方防标签页泄漏、请求 source-path/语言参数等修复。 |

## 保留的本地能力

TUI 启动器、单端口控制台、分镜与角色生图、参考图生图及分页、统一参考图库、双语提示词详情、本地媒体缓存及离线浏览、自定义 LLM 配置与测试、材质和画幅设置、现有项目数据库及设置。

## 检查

- 后端编译检查、前端构建通过；完整 Python unit suite：392 passed。
- 扩展冷启动、被动刷新不新建标签页、精确选择新建标签页、专用工作标签页回归检查。
- 新旧 API 共存测试使用 mock，不触发付费生成。
- 为官方测试修正 Windows 路径表示和 UTF-8 fixture 编码，未放宽原有行为断言。
- 实际服务健康版本为 1.3.1，两个已关联项目再次同步 508 条媒体成功、失败数 0。媒体库共 652 条有效缓存，远端未缓存数 0。
- 官方 `/settings` 配置页可用，本地 `/settings/llm` 与生图工坊/图库导航均保留；`settings.json` 与备份的 SHA-256 一致。
- 扩展目录已更新到 0.3.2。浏览器安全策略阻止自动打开扩展管理页，已请求用户手动重载扩展并刷新 Flow 标签页；未绕过该限制。

生成模型的实收费端到端调用不在本次回归范围内；相关官方行为由其测试及合并兼容测试覆盖。
